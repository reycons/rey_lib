"""rey_lib — shared reusable library.

Sub-packages
------------
config      YAML config loader, Namespace builder, context lookup helpers.
logs        Centralised logging setup with call-depth indentation.
errors      Generic base exception, error handler, input validators.
ftp         FTP/FTPS/SFTP client, state manager, sync engine.
db          DuckDB connection and SQL execution layer.
files       CSV / XLSX reading and writing utilities.
"""
